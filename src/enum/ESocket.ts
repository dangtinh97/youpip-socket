enum ESocket {
    PING="PING",
    JOIN_ROOM = "JOIN_ROOM",
    MESSAGE = "MESSAGE",
    LEAVE_ROOM = "LEAVE_ROOM",
    SEND_DATA = "SEND_DATA",
    JOIN_CALL = "JOIN_CALL"
}

export default ESocket
